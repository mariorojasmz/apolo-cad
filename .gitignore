.venv/
__pycache__/
*.pyc
*.egg-info/
node_modules/
ui/node_modules/
ui/dist/
.pytest_cache/
logs/
# SOLO el data/ de la RAÍZ (SQLite de proyectos). OJO: la regla genérica "data/"
# ignoraba también core/apolo/library/data/ (el CATÁLOGO YAML, código fuente).
/data/
.claude/settings.local.json

# volcados de runtime en la raíz (curl de depuración)
/scene.json
/connectivity.json

# Datasheets de fabricante (referencia local; copyright del proveedor, NO redistribuir en el repo público)
docs/datasheets/*.pdf
ui/tsconfig.tsbuildinfo

# Paquetes benchmark: conservar el TEXTO (md/json/csv) y los PDF legibles como testigo
# diffeable; ignorar los binarios pesados y regenerables (STEP/DWG/renders — geometría e
# imágenes que git no sabe diffear y que se regeneran con benchmark_package.py)
docs/benchmark/**/*.step
docs/benchmark/**/*.dwg
docs/benchmark/**/*.zip
docs/benchmark/**/render/
