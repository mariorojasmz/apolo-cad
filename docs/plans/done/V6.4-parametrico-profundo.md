# PLAN V6.4 — Paramétrico profundo: faja 38 100% paramétrica + tablas de diseño

> **Lee ENTERA la guía antes de tocar nada.** Cuarto ítem del roadmap V6 (madurez
> paramétrico 5→6.5). Criterio de «hecho» V6: usable por chat/MCP + schema-driven +
> tests + E2E real **+ tortura y health verdes**. Este plan tiene una parte de
> MOTOR (pequeña), una de CIRUGÍA del proyecto real 38 (el grueso) y una de
> PRODUCTO (tablas de diseño sobre infraestructura existente).

## Contexto (verificado en código y en el log real del 38, 2026-07-10)

**El motor ya es más capaz de lo que CLAUDE.md dice:**
- La resolución de `=expr` es **uniforme y recursiva sobre TODO el árbol de params**
  de TODO comando (`registry.py:1694-1719` `validate_params` → `resolve_params`
  antes de pydantic). No hay campos numéricos sin soporte: position/rotation/
  origin/axis de juntas, value de mates, overrides, translate de transform — todo
  acepta expresión. Los "huecos" son de DISCIPLINA de autoría, no de mecánica.
- **`run_script` SÍ ve las variables del proyecto**: el executor recibe
  `resolve_all(variables)` (`registry.py:1776`, `1318-1322`) y el sandbox inyecta
  `namespace["V"]` (`script_wrapper.py:30-31`). Un script puede leer `V["largo"]`
  y cascadear. **CLAUDE.md:464-465 está DESACTUALIZADO** («los run_script NO ven
  las variables») — corregirlo es parte de este plan. El problema de la faja es
  que sus scripts fueron ESCRITOS con literales (`Pos(3806,…)`).
- Variables con referencias cruzadas y detección de ciclos (`resolve_all`,
  `expressions.py:92-123`). **Límite duro: SIN condicionales** — no hay ternario,
  comparadores ni booleanos (test que lo prohíbe: `test_expressions.py:53`).
- **`Document.configurations` está VIVO end-to-end** y es exactamente «variante →
  {variable: expresión}»: `save/apply/delete_configuration` (document.py:647-672,
  apply = un solo undo), API (`main.py:1231-1243`), UI (`VariablesDialog.tsx:
  122-159`), persistido en manifest, tests (`test_product.py:56-169`). Falta:
  vista de TABLA (variable × variante), edición de una variante sin aplicarla, y
  superficie MCP (no hay tool de configuraciones).
- **requirements ⟂ variables**: desconectados. NO construir un puente implícito
  (`=req.carga_kg`): los requirements son metadato de manifest FUERA del log — no
  cambian las firmas del regenerate → geometría stale silenciosa. El puente debe
  ser EXPLÍCITO (importar un requisito como `set_variable`).

**Auditoría del log real de la faja 38** (script read-only sobre `data/apolo.db`):
- **701 comandos, y ~420 son ESCOMBRO de UI**: el tramo c708–c1113 son bocetos/
  cajas creados en el viewport, arrastrados con cientos de `transform` y borrados
  — 321 de los 335 transforms apuntan a features MUERTAS. Basura viva: c1099
  («Boceto» 200×200×45 estacionado junto al motor) + sus 14 transforms, c765
  («Viga trazada» flotante), c701/c702 (par de transform_group neto cero). Nada
  vivo referencia a los muertos (fasten/ground verificados) → poda segura.
- **Las 35 variables ya existen y están BIEN diseñadas** (22 base + 13 derivadas
  en cascada). Los literales mágicos COINCIDEN con variables existentes:
  3806 ≡ `long_centros`, 791 ≡ `drum_cz`, 390 ≡ `larg_inner_y`, orígenes de
  j_trav = `100+k·(long_centros−200)/4` exacto. Casi no hacen falta variables
  nuevas — hace falta USARLAS.
- Parametricidad del modelo vivo (295 comandos): **27% de campos numéricos con
  expresión**. La estructura está bien atada; los residuos: ~50 ediciones
  mecánicas (cylinders del lazo de banda r=59/57→`=rad_tambor±2`, posiciones del
  conjunto motriz x=3806→`=long_centros` z=791→`=drum_cz`, orígenes de las 15
  juntas, take_up, ménsulas de retorno z=737.5) + **los 6 run_script del conjunto
  motriz** (c647 pies, c669 tambor motriz, c673 disco anti-giro, c685 ménsulas
  chumacera, c703/c704 ménsula motor + tornillería) con ~60 coordenadas horneadas.
- `fasten` con size/throat/length literales (105 campos): es DIMENSIONAMIENTO de
  ingeniería — defendible como literal, NO re-parametrizar.

## Fase A — Motor: condicionales en expresiones (commit `feat: V6.4a`)

Habilitar en `expressions.py` (lista blanca AST, mismo estilo): `ast.IfExp`
(ternario `a if cond else b`), `ast.Compare` (`< <= > >= == !=`, encadenados) y
`ast.BoolOp` (`and`/`or` sobre comparaciones). Motivación: tablas de diseño con
lógica («`=3 if largo_total>3500 else 2`» soportes según largo). Mantener FUERA:
strings, `in`, `is`, lambdas, walrus (los tests de seguridad existentes
`test_expressions.py:44-58` deben seguir verdes — actualizar SOLO el caso del
ternario, que pasa de prohibido a permitido). Actualizar
`GET /api/expression-grammar` (main.py:2016-2032) y el hint del VariablesDialog.
Tests: ternario/comparadores/booleanos + los rechazos que siguen vigentes +
división por cero dentro de la rama no tomada (el AST evalúa perezoso: `ev()`
del ternario debe evaluar SOLO la rama elegida — implementarlo así).

## Fase B — Cirugía: faja 38 100% paramétrica (commit `feat: V6.4b`, o varios)

⚠️ **Es el proyecto REAL del usuario. Antes de tocar: `save_revision`** («pre
V6.4») y verificar que la revisión quedó en la BD. Trabajar por MCP/API contra la
API corriendo (NO scripts offline que importen apolo.* — recompilan .pyc y
recargan el worker). Lotes grandes → `run_batch`/`edit_batch` (timeout HTTP).

1. **Poda del escombro** (`POST /api/commands/remove`, atómico): los ~407
   comandos muertos + c1099 con sus 14 transforms + c765 + c701/c702. En 2-3
   lotes. Verificación tras cada lote: `total_solidos == 82` (sin bajas vivas),
   volúmenes de 5 piezas testigo idénticos, `check_integrity` limpio, soundness
   sin flotantes nuevos. El log queda en ~275 comandos (bonus de perf de replay).
2. **edit_batch de literales→expresión** (~50 ediciones, la lista del top-15 de
   la auditoría es el índice): posiciones del conjunto motriz (c670 eje, c682
   NMRV, c686/687 chumaceras → `=long_centros`/`=drum_cz`), radios del lazo de
   banda (c111/112/115/116, c120/121), orígenes de las 15 juntas (j_mesa*,
   j_trav*, j_tensor_cola → expresiones que la auditoría ya verificó
   numéricamente), take_up c412, ménsulas c339-342, pies... **Regla: la expresión
   debe resolver EXACTAMENTE al literal actual** (verificar con
   `resolve_expression` antes de editar; la geometría no debe moverse ni 0.001 mm
   — comparar volúmenes/bboxes antes/después de cada lote).
3. **Los 6 run_script**: reescribirlos para leer `V[...]` (el mecanismo YA
   existe — es autoría). `edit_command` del campo `code` sustituyendo cada
   coordenada horneada por su expresión sobre `V` (3806→`V["long_centros"]`,
   791→`V["drum_cz"]`, etc. — la auditoría mapeó las equivalencias). Validar cada
   uno primero con `test_script` (dry-run: volumen/bbox idénticos al actual).
   NO convertirlos a comandos del registro (re-modelar es más riesgo sin ganancia
   — el script paramétrico cascadea igual).
4. **E2E de ACEPTACIÓN (el caso que hoy falla)**: `set_variable largo_total=3200`
   → regenerar → el conjunto motriz ENTERO sigue al extremo motriz (tambor, eje,
   chumaceras, NMRV, ménsulas, disco — medir que sus x quedaron en el nuevo
   `long_centros`), juntas en su sitio, `check_interference` sin colisiones
   nuevas, soundness sin flotantes, render de evidencia. Repetir con
   `ancho_banda=500`. **Restaurar largo_total=4000/ancho_banda=600 al terminar**
   (es el proyecto real) y `save_revision` («V6.4 paramétrica»).

## Fase C — Tablas de diseño (commit `feat: V6.4c`)

Sobre la infraestructura viva de `configurations`:
1. **Edición explícita de una variante**: endpoint `PUT /api/configurations/
   {name}` con `{variable: expresión}` explícito (hoy `save` solo captura el
   snapshot actual). Validar: variables existen, expresiones parsean, sin ciclos.
   `apply` sigue igual (un undo).
2. **Vista de TABLA en la UI** (`VariablesDialog.tsx`): grilla variables (filas)
   × configuraciones (columnas), celdas editables (escriben vía el PUT), columna
   activa aplicable con un clic, botón «nueva variante desde la actual». Sin
   librerías nuevas — tabla HTML como el resto del diálogo.
3. **Superficie MCP** (los huecos del MCP son de LECTURA/ergonomía): tools
   `save_configuration(name)` / `apply_configuration(name)` (finas, pegan a los
   endpoints; 66→68 tools — **reiniciar host MCP**). Las configuraciones ya salen
   en el brief del documento.
4. **Puente requirements→variables EXPLÍCITO**: botón/acción «usar como
   variable» en el panel Requisitos (crea el `set_variable` con el valor del
   requisito — p. ej. `carga_max = carga_kg`). NO hacer puente implícito (razón
   arriba: invariante de firmas del regenerate). Documentar la razón en el código.
5. E2E: crear variantes «4m estándar» y «3.2m compacta» de la faja 38, alternarlas
   por MCP y verificar que el modelo salta completo entre ambas (piezas testigo
   medidas). Con la Fase B hecha, esto ES la demo de tablas de diseño.

## Fase D — Cierre (en el último commit)

- **Corregir CLAUDE.md:464-465** (run_script SÍ ve variables vía `V`; el gap de
  la faja era de autoría — reescribir la lección). Actualizar sección de
  expresiones (condicionales), configuraciones (tabla + PUT + tools), conteos
  (tests/68 tools), V6.4 HECHO (5→6.5), § Pendientes: el patrón sistémico
  «drag&drop emite transform literal» queda ANOTADO como follow-up de UI (fuera
  de alcance aquí), igual que import/export CSV de la tabla.
- Devlog: narrativa con los números de la cirugía (701→~275 comandos, 27%→%
  final de expresiones, evidencia del E2E de largo_total).
- Baseline: el open de la faja cambia por la PODA (menos comandos) — regenerar
  `docs/perf_baseline.json` en la máquina del usuario y anotar que la fuente
  cambió (log podado).

## Qué NO hacer

- NO tocar la faja sin `save_revision` previa verificada.
- NO re-modelar los run_script como comandos del registro (editar su `code`).
- NO re-parametrizar el dimensionamiento de `fasten` (throat/length/qty son
  ingeniería, no geometría dependiente).
- NO puente implícito requirements→variables (rompe el invariante de firmas).
- NO strings/`in`/lambdas en expresiones — solo ternario/comparadores/booleanos.
- NO dejar el proyecto 38 en un estado distinto al original (4000/600) al cerrar.

## Gotchas de la casa

- La cirugía corre contra la API viva: si corre con `--reload`, NO tocar `.py`
  mientras editas el doc (reload → doc en memoria blanqueado; el autosave ya
  guardó — `open_project(38)` recupera). Zombie-socket :8000 si algo «no surte
  efecto».
- `edit_command` MCP hace PATCH (merge superficial) pero un sub-objeto position
  se reemplaza ENTERO (reenviar x,y,z los tres).
- La poda invalida la caché de geometría del 38 (las firmas cambian) — el primer
  open tras la cirugía será frío y se re-cachea solo; no es un bug.
- `pytest tests -q` + `-m torture` tras cada fase de CÓDIGO (A y C); la Fase B es
  datos (sin suite nueva, su verificación es el E2E). `cd ui ; npm run build`
  tras la Fase C.
- Host MCP: reiniciar SOLO al final de la Fase C (tools nuevas).
