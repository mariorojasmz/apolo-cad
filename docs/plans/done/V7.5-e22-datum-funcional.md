# V7.5 — E2.2: datum por cara FUNCIONAL + barrenos del UCP en el modelo

**Objetivo**: cerrar el residual top del entregable más débil (planos 73.2 %): la rúbrica
E2.2 exige «Datums = caras de FUNCIÓN/montaje» y hoy el datum es la esquina inf-izq del
bbox (fallback honesto declarado en V7.2); además la ménsula de chumacera del 38 NO tiene
los barrenos del UCP en el MODELO (gap E1.1: el UCP no puede atornillarse — por eso su
lámina no recibe callouts). Dos frentes: cirugía de modelo (38) + criterio de datum en el
drawing. **No subir nota sin medir**: la medición va en el próximo re-benchmark.

**Reconocimiento ya hecho (anclas exactas, verificadas 2026-07-22)**:
- `c685` = run_script «Ménsulas de chumacera (lapa bajo el larguero)»: UN feature con DOS
  placas `Pos(V["long_centros"], ±388, 736.9) * Box(182, 106, 13)` (paramétrico solo en x).
- UCP207 encima (`c686`/`c687`, catálogo): **J=127 mm** entre pernos, agujero base **N=17**,
  base L=167×A=48. Perno típico **M14** (broca de paso ISO 273 media = **Ø15.5**).
- Las uniones YA están declaradas con criterio: `c690/c691` fasten **perno M14 qty=2**
  (chumacera↔ménsula) y `c692/c693` **soldadura a=4/L=100** (ménsula↔larguero c93/c95).
  Falta la GEOMETRÍA (barrenos + pernos físicos), no la declaración.
- Drawing: único call site `sheet.py:514` `auto_hole_dims(model, view, rect, tx,
  datum=shop_notes)`; la esquina se marca en `autodim.py:27-29` (`datum_flag` en inf-izq).
  Plomería por pieza ya modelada por `piece_fits` (`sheetset.py:97` y `:187`, mapa por
  `r["_rep"]`) — espejarla.
- `PERNO-HEX-M14` existe en catálogo.

---

## A · Modelo 38: barrenos + pernos del UCP (cirugía acotada)

1. **Medir ANTES de taladrar** (no asumir): con `near`/`get_topology`/`measure` verificar
   la orientación real del UCP207 (que su línea de pernos corre en X, centrada en
   `x=long_centros`) y el espesor de su base (para el largo del perno). Si la base no está
   centrada sobre la placa, centrar los barrenos bajo el UCP REAL.
2. **4× `drill_hole` EN SITIO sobre `c685`** (conserva id — NUNCA boolean_op: hay
   fasteners que lo referencian): Ø15.5 pasantes en z, en
   `x = "=long_centros - 63.5"` / `"=long_centros + 63.5"` (J=127), `y = ±388` (literal,
   la MISMA constante del script — documentar como V6.4d). GOTCHA: `c685` es un feature
   con DOS sólidos (compound del script) — verificar que `drill_hole` corta el compound;
   si el executor exigiera sólido único, el fallback es partir el script en dos comandos
   `bracket(+1)`/`bracket(-1)` (cirugía mayor: remap de fasteners c690-693 — evitar si el
   compound traga).
3. **4× `insert_component` `PERNO-HEX-M14`** en los ejes de los barrenos (largo comercial
   según grip medido: placa 13 + base UCP; ~M14×50 esperado). Sin los pernos físicos, el
   lint «barreno de paso sin perno en su eje» dispararía 4 avisos nuevos. Hardware →
   fuera de lista de corte/interferencia automáticamente. NO duplicar el fasten (c690/691
   ya dimensionan M14×2).
4. Ejecutar por `run_batch` con **contrato** (`expect`): tras el lote, 4 barrenos
   presentes (get_topology de c685), lints pre-entrega sin avisos NUEVOS, `verify` de que
   nada más se movió. La cédula de herraje del juego debe ganar los M14.

## B · Datum por cara de FUNCIÓN (drawing, señal = fasteners)

1. **Derivación en la capa API** — `_piece_datum_sides(doc) -> {feature_id: side}` con
   side ∈ {"+x","-x","+y","-y","+z","-z"}: para cada pieza FABRICADA con uniones
   declaradas, tomar la de mayor peso (soldadura > perno > contacto), calcular el eje de
   contacto por solape de bboxes (mismo patrón del anclaje de símbolos de soldadura en
   `compose_sheet`): el eje de solape mínimo ≈ normal de la cara de contacto; `side` =
   lado de la pieza que MIRA a la contraparte. SIN señal → la pieza no entra al mapa
   (fallback intacto). PROHIBIDO inferir por nombre (lección V7.2c de los brackets).
2. **Plomería espejo de `piece_fits`**: `sheet_set(piece_datums={fid: side})` →
   `compose_sheet`/`sheet.py` → `auto_hole_dims(..., datum_side=None)`. El default None
   preserva el comportamiento actual byte-cercano (láminas sin señal, GA, compat).
3. **En `autodim.py`**: si `datum_side` se proyecta como una ARISTA en la vista, el
   origen de las `baseline_dims` se mueve a esa arista (la esquina del lado funcional) y
   el `datum_flag` «A» se dibuja al CENTRO de esa arista; si la cara funcional es ⊥ a la
   vista (se proyecta como el plano completo) → fallback esquina actual. Honesto: datum
   funcional SOLO cuando la vista lo permite.
4. La ménsula queda de testigo: cara funcional = TOPE (+z: recibe el UCP y solda bajo el
   larguero) → flag en la arista superior del alzado, posiciones de barreno acotadas
   desde ella.

## C · Tests

- `_piece_datum_sides` PURO: doc sintético — placa con caja atornillada encima → "+z";
  soldada a un costado → ese lado; soldadura gana a contacto; sin uniones → fuera del
  mapa.
- autodim con `datum_side`: lámina sintética → el flag y el origen de baseline salen de
  la arista correcta (assert por coordenadas en el `SheetModel`, patrón de los tests de
  drawing existentes); `datum_side=None` → byte-cercano al actual (regresión).
- Cara ⊥ a la vista → fallback esquina (sin flag desplazado).
- Modelo (E2E de test si es barato, si no queda en D): 4 círculos Ø15.5 en `c685` +
  cédula con M14.

## D · Cierre

1. **E2E vivo del 38**: regenerar el juego de planos; verificar POR TEXTO DE PDF (pypdf,
   protocolo de re-auditoría) que las sub-láminas (k/n) de la ménsula traen los 4
   barrenos rotulados con posición desde el datum funcional + flag «A» en la arista de
   contacto, y que la cédula ganó los 4 M14. `predelivery_lints` = 0 avisos nuevos;
   `engineering_check` sigue 0 avisos.
2. **Benchmark**: anotar en el calificacion.md vigente que E2.2 quedó atacado (nota de
   capacidad, SIN subir el número) — la medición real va en el re-benchmark con
   rúbrica-v2 (que puntuará también stack-up V7.3 y FEA V7.4).
3. CLAUDE.md (§ Planos «último kilómetro»: datum funcional por fasteners + gotcha del
   compound; § modelo 38 si cambió el conteo de sólidos por los pernos) + devlog + plan →
   `done/` + COMMIT.

**Gotchas para el implementador**:
- El server con `--reload` NO debe correr durante las ediciones; la cirugía del 38 va por
  HTTP puro (run_batch con contrato), JAMÁS importando `apolo.*` con el server vivo.
- `drill_hole` sobre pieza referenciada por fasteners es seguro (muta en sitio); lo
  prohibido es `boolean_op` (reasigna id).
- `datum=shop_notes` (bool) se mantiene; `datum_side` es param NUEVO opcional — no romper
  la firma existente ni el GA.
- El sufijo «(k/n)» de láminas multi-sólido ya existe (V7.2c) — las dos placas de c685
  comparten `_rep`; `piece_datums` mapea por feature_id igual que `piece_fits`.
- Autosave: tras la cirugía, verificar `GET /api/health` y que el proyecto persiste antes
  de regenerar planos.
