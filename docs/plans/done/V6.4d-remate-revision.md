# PLAN V6.4d — Remate de la revisión de V6.4

> V6.4 (a/b/c) quedó implementado y aprobado: condicionales impecables (revisión
> adversarial con ~30 repros propios), cirugía real de la faja (701→328 comandos,
> el motriz entero cascadea desde `long_centros` — verificado en la BD por un
> auditor independiente), tablas de diseño sólidas, suite 1039 + tortura 15
> verdes. La revisión encontró **residuos concretos entre el plan y la
> ejecución** que este remate cierra. Referencia:
> `docs/plans/done/V6.4-parametrico-profundo.md`.

## Fix 1 — Baseline de perf STALE (el único incumplimiento directo del plan)

`docs/perf_baseline.json` sigue siendo el de V6.2d: dice `faja_comandos: 701` y
`open_frio_faja_s: 3.15`, pero la poda dejó el proyecto 38 en **328 comandos** y
`scripts/perf_baseline.py` carga el proyecto REAL de la BD → la fuente cambió y
la próxima comparación de perf hará ruido. **Acción**: correr
`scripts/perf_baseline.py` y commitear el JSON, anotando en la `nota` que la
fuente es el log PODADO de V6.4. ⚠️ Gotcha: el script importa `apolo.*` →
recompila `.pyc`; si la API corre con `--reload` eso recarga el worker y
blanquea el doc en memoria. Correrlo con la API detenida (o sin `--reload` y
reabrir el proyecto después).

## Fix 2 — Faja 38: los orígenes de las 15 juntas siguen 100 % literales

El hueco NO documentado más relevante (80 campos literales; el plan Fase B paso
2 los exigía y ni devlog ni CLAUDE.md lo anotan). Con `save_revision` previa
(«pre V6.4d») y por MCP/API viva, `edit_batch`:
- **j_trav1..5**: `origin.x = "=100 + k*(long_centros-200)/4"` (k=0..4) — la
  auditoría PRE verificó esta fórmula EXACTA contra los literales
  (100/1001/1903/2804/3706).
- **j_tensor_cola**: `origin.z = "=drum_cz"` (791).
- **j_mesa1..4** (x=550/1452/2354/3255, z=847) y las z de j_trav (826): derivar
  la expresión candidata y **verificarla con `resolve_expression` — solo editar
  si resuelve EXACTO al literal actual** (regla V6.4b). Si alguna no tiene
  expresión limpia (espaciado ad-hoc), NO inventar una falsa: dejarla literal y
  ANOTARLA en CLAUDE.md § Pendientes con la razón.
- Recordar: `edit_command` MCP mergea, pero un sub-objeto `origin` se reemplaza
  ENTERO (mandar x,y,z los tres).

## Fix 3 — Faja 38: residuos dimensionales del top-15

`edit_batch` (misma regla resolver-exacto-antes-de-editar):
- c111/c112 `radius=59` → `"=rad_tambor+2"` · c115/c116 `radius=57` →
  `"=rad_tambor"` (rad_tambor=57; el 59 es el radio + espesor de banda —
  CONFIRMAR la semántica con las variables antes de atar; si 59 ≠ rad+esp_banda
  exacta, atar a la que resuelva exacto).
- c120/c121 `radius=25` → `"=diam_rodillo/2"` si existe la variable (verificar);
  sus `z=707` y c339-342 `z=737.5` → expresión sobre `drum_cz`/`z_mesa_*` solo
  si resuelve exacto.

## Fix 4 — Faja 38: podar la basura viva declarada

`POST /api/commands/remove`: c1099 («Boceto» 200×200×45 junto al motor) + sus 14
transforms + c765 («Viga trazada» flotante). Verificar después: sólidos 82→80
(ambos son piezas basura VISIBLES — confirmar el conteo esperado ANTES de podar
mirando la escena), `check_integrity` limpio, soundness sin cambios en lo real.

## Fix 5 — Guías de boceto huérfanas (código + datos)

El manifest del 38 retiene **25 `sketch_guides` de comandos podados** (metadato
huérfano benigno). Dos acciones: (a) CÓDIGO: en la carga
(`from_apolo_bytes`) o en `remove_commands`, podar las entradas de
`sketch_guides` cuyo comando ya no existe (elegir el punto que NO rompa el
contrato de metadatos; test con guía huérfana sintética); (b) DATOS: limpiar las
25 del proyecto 38 (la carga con el fix (a) puede hacerlo sola — verificar).

## Fix 6 — E2E de cierre

Con juntas atadas: `apply_configuration("3.2m compacta")` → verificar que las
JUNTAS siguen (j_trav5 dentro del bastidor encogido, j_tensor en el nuevo
`drum_cz`), 0 colisiones nuevas, render de evidencia → volver a
`apply_configuration("4m estandar")` y `save_revision` («V6.4d»). Pegar medidas
al devlog.

## Menores (hacer si salen gratis; si no, anotar)

- Nota en `GET /api/expression-grammar`: `and`/`or` colapsan a 1.0/0.0 (el idiom
  `x or 5` NO devuelve 5).
- Grilla de variantes: celda vaciada no hace nada (documentado, no bug).
- Las tools MCP `save/apply_configuration` no tienen evidencia de ejecución en
  vivo (el E2E fue por API) — usarlas en el Fix 6 las estrena de paso.

## Criterio de hecho

Suite + tortura verdes (el Fix 5a es el único cambio de código con test nuevo) ·
la faja termina en «4m estandar» (largo 4000/ancho 600) con revisión guardada ·
baseline commiteado con fuente anotada · CLAUDE.md: actualizar el follow-up (4)
de V6.4 con lo que quede vivo y los conteos si cambian · commit sugerido:
`fix: V6.4d — juntas paramétricas + poda residual + baseline + guías huérfanas`
· mover este plan a `done/` al cerrar.
