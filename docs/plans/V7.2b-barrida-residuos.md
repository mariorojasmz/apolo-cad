# V7.2b — Barrida de residuos baratos (manual · normas · lints · proceso)

**Objetivo medible**: cerrar los residuos que las re-auditorías de V7.1/V7.1c/V7.2
dejaron rankeados, subiendo el global re-auditado de **~73 % → ~78-80 %** de nivel
despacho. Entregables objetivo: **manual E5 2.00→~3 (50→75 %)**, **memoria E3.3 3→4
(E3 80→~85-90 %)**, **E1 81→~87 %** (mata los 2 avisos vivos), **E2.5 2.5→3** (rama
sierra). Todo son fixes acotados; ninguno toca kernel/doc/regenerate.

**Reglas de la casa**: cirugía del modelo 38 por API viva (jamás importar `apolo.*`
offline con `--reload`); suite + tortura verdes; re-benchmark a carpeta fechada NUEVA
(los testigos previos no se pisan); re-calificar SOLO los criterios tocados con la
rúbrica-v1; la nota debe ser VERDAD (si algo no llega, se declara). Commit(s) al final.

---

## A · Manual por GRAFO DE SOPORTE (E5.1 2→3) — la brecha top

Hoy `assembly_steps` (`drawing/assembly_manual.py:53`) deriva la secuencia del ORDEN
DEL LOG paginado por grupos → las chumaceras (paso 6) se montan DESPUÉS del motor
(paso 5) cuando soportan el eje del paso 3; los pernos de anclaje van tarde.

1. **Orden por soporte**: usar el grafo de soporte DIRIGIDO que YA existe
   (`assembly/connectivity.py::detect_structure` — semilla `grounds`, aristas
   soporta→soportado): orden topológico desde tierra (lo que toca piso primero;
   una pieza se monta DESPUÉS de todo lo que la soporta). Dentro del mismo nivel
   topológico, conservar el agrupado por sub-ensamblajes actual (no romper la
   paginación por grupos de V5.2). Piezas sin arista de soporte → heurística actual
   (fallback declarado).
2. **Texto específico por paso**: sustituir la instrucción genérica repetida
   («Monta y fija estas piezas…») por texto según la FAMILIA del paso y sus uniones
   (datos que ya están en el paso): perfiles soldados → «Presentar, escuadrar y
   soldar (cordón a=X/L=Y donde esté dimensionado)»; herraje atornillado →
   «Atornillar con {n}×{size} {norma}, apretar en cruz»; chumaceras → «Montar sobre
   el eje, alinear y atornillar a la base»; catálogo → «Instalar según fabricante».
   8-10 plantillas cubren el vertical; sin señal → texto genérico actual.
3. **Fusionar pasos huérfanos**: un paso con 1 pieza y sin grupo propio se fusiona al
   paso del sub-ensamblaje al que se atornilla/suelda (su vecino en el grafo).
4. Tests: modelo sintético pata→larguero→chumacera→eje→motor con grounds → el orden
   respeta el soporte (chumacera ANTES que su eje, eje antes que el motor que cuelga
   de él); huérfano fusionado; fallback sin conectividad = comportamiento actual.

## B · Norma citada en las 15 verificaciones cuantitativas (E3.3 3→4)

Hoy 4/15 citan norma (CEMA ×2, ISO 5048/DIN 22101, Euler-Eytelwein — campo `norma`
del bloque `calc` en `library/rules.py`, p. ej. `:642,:667,:823`). Completar las 11
restantes con la referencia REAL de cada método (no inventar citas):

- L10 rodamientos → **ISO 281** · pernos → **EN 1993-1-8 / ISO 898-1** (ya en el
  motor de pernos; citarla en el calc) · soldaduras → **EN 1993-1-8** (garganta) ·
  pandeo de patas → **Euler (EN 1993-1-1 §6.3 como marco)** · flexión/flecha de
  bastidor → criterio de serviciabilidad **L/240 (práctica AISC)** · flexión de eje →
  **criterio σ_adm = 0.6·σy (ASME B106.1M como referencia)** · vuelco → equilibrio
  estático (declarar «criterio de diseño») · velocidad/capacidad → **CEMA**.
- **Regla de honestidad**: donde el método es criterio de diseño y no norma publicada,
  el campo dice «criterio de diseño: {cuál}» — NUNCA una cita inventada. La memoria
  pinta «NORMA DE REFERENCIA» ya existente (V5.10) — solo faltan los datos.
- Test: las 15 cuantitativas del check de la faja sintética llevan `norma` no vacía.

## C · Lints pre-entrega (habrían cazado solos los defectos del 38)

En `library/` (función pura estilo `verify.py`) + expuesto en `/api/checks` y en el
`engineering_check` (sección «pre-entrega»):

1. **«Barreno sin perno»**: taladro pasante Ø7-20 mm sin fastener perno cuyo eje pase
   cerca (tolerancia por Ø) → aviso con la pieza y posición. Habría cazado los 5
   pernos faltantes ANTES del benchmark.
2. **«Pieza sin grupo NI unión declarada»**: feature sin grupo y sin arista en
   conectividad → aviso (habría cazado `c704`). Excluir guías/superficies de
   construcción.
3. Tests con los dos escenarios sintéticos (placa 4 barrenos/3 pernos → 1 aviso;
   caja suelta sin grupo → 1 aviso; modelo sano → 0).

## D · E1 residual: catálogo y fits que matan los 2 avisos vivos

1. **Pernos de anclaje → catálogo DIN 933** (cirugía del 38 por API): reemplazar el
   comando de los 24 pernos a-medida (`c147`/`c1114`) por inserciones de catálogo
   PERNO DIN 933 M12 equivalentes (mismas posiciones paramétricas; recuerda: el
   sub-grafo se canjea completo con `commands/remove` + los nuevos, y los fasteners
   auto se re-declaran). BOM debe pasarlos de «fabricación 0.28 USD» a catálogo.
2. **Fit del eje del tensor**: los dos 6207 montan en el eje fijo Ø35 sin ajuste
   declarado (los 2 AVISOS de la memoria). Declarar el asiento SEGÚN
   `SEAT_RECOMMENDATIONS` (`library/engineering/fits.py`) para anillo INTERIOR
   estacionario / carga rotatoria exterior (típico g6/h6 — consultar la tabla, no
   asumir k6 de prensado). El fit va en el NOMBRE del eje («… Ø35 g6») → la regla de
   asiento lo reconoce y el plano lo rotula solo. Resultado: memoria con 0 avisos.
3. Verificar `engineering_check` → «APROBADO» limpio y guardar revisión.

## E · Proceso e inferencias (E2.5 2.5→3, brechas de la re-auditoría V7.2)

En `drawing/process.py::infer_process`:
1. **«Sierra» para miembros de weldment**: hoy la rama exige `component.category` y
   los miembros de weldment no lo llevan → caen a «mecanizado Ra 6.3». Fix: pasar la
   categoría del perfil del weldment al Feature (o inferir: sección constante +
   `cut_length`/`miter` presentes = perfil aserrado). Un tubo estructural rotula
   «corte en sierra · acabado de laminación» (sin Ra inventado o Ra 12.5).
2. **«Plegado» solo con pliegue real**: la repisa plana de 6 mm decía «+ plegado»;
   la señal debe ser chapa CON flaps/pliegues (comando create_sheet_metal con flaps o
   lados), no todo espesor ≤6 mm.
3. **Torneado por asiento**: el eje del tensor tiene asiento con fit pero proceso
   «mecanizado» porque su nombre no traía el token — si una pieza tiene fit ISO 286
   declarado (mapa de la capa API), su proceso es torneado aunque el nombre no lo diga.
4. **Evidencias del benchmark = página real**: los PNG de `evidencia/` deben ser la
   página del PDF entregable (si hay `pymupdf`/`fitz` en el venv, rasterizar; si no,
   renderizar el MISMO SheetModel de esa lámina a PNG — jamás un re-render con
   metadatos distintos: el «Plano —» delató al de V7.2).
5. Tests: weldment sintético → «sierra»; placa sin pliegues → sin «plegado»; pieza
   con fit → torneado.

## F · Verificación, re-benchmark y cierre

1. Suite + tortura verdes (ampliadas con los tests de A/B/C/E).
2. Re-benchmark: `scripts/benchmark_package.py --project 38 --expect largo_total=4000`
   a `docs/benchmark/faja-paqueteria-4m/<fecha>/`. Re-calificar SOLO E5 (completo),
   E3.3/E3.4, E1.1/E1.2 y E2.5 contra rubrica-v1, con evidencia; el resto hereda la
   nota re-auditada de V7.2 (~73 %). Esperado: global ~78-80 % — si no llega,
   declararlo con la brecha residual.
3. CLAUDE.md (Veredicto por RESULTADOS + V7.2b HECHO en hoja de ruta + conteos) +
   devlog + plan a `done/` + COMMIT (no dejar el trabajo sin commitear).
4. Nota MCP: sin tools nuevas → sin reinicio de host (salvo que A cambie la firma de
   `assembly_manual`, que no debería).
